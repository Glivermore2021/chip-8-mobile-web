#include "chip8.h"
#include <string.h>
#include <stdio.h>
#include <stdlib.h>

static const uint8_t fontset[80] = {
  0xF0, 0x90, 0x90, 0x90, 0xF0, // 0
  0x20, 0x60, 0x20, 0x20, 0x70, // 1
  0xF0, 0x10, 0xF0, 0x80, 0xF0, // 2
  0xF0, 0x10, 0xF0, 0x10, 0xF0, // 3
  0x90, 0x90, 0xF0, 0x10, 0x10, // 4
  0xF0, 0x80, 0xF0, 0x10, 0xF0, // 5
  0xF0, 0x80, 0xF0, 0x90, 0xF0, // 6
  0xF0, 0x10, 0x20, 0x40, 0x40, // 7
  0xF0, 0x90, 0xF0, 0x90, 0xF0, // 8
  0xF0, 0x90, 0xF0, 0x10, 0xF0, // 9
  0xF0, 0x90, 0xF0, 0x90, 0x90, // A
  0xE0, 0x90, 0xE0, 0x90, 0xE0, // B
  0xF0, 0x80, 0x80, 0x80, 0xF0, // C
  0xE0, 0x90, 0x90, 0x90, 0xE0, // D
  0xF0, 0x80, 0xF0, 0x80, 0xF0, // E
  0xF0, 0x80, 0xF0, 0x80, 0x80  // F
};

void chip8_init(Chip8* chip) {
    memset(chip, 0, sizeof(Chip8));
    chip->pc = 0x200;
    memcpy(chip->memory, fontset, sizeof(fontset));
}

void chip8_load_rom(Chip8* chip, const uint8_t* rom, size_t size) {
    memcpy(&chip->memory[0x200], rom, size);
}

void chip8_cycle(Chip8* chip) {
    chip->opcode = chip->memory[chip->pc] << 8 | chip->memory[chip->pc + 1];
    chip->pc += 2;

    // Example: Clear screen (00E0)
    if (chip->opcode == 0x00E0) {
        memset(chip->video, 0, VIDEO_WIDTH * VIDEO_HEIGHT);
    }

    // ... add more opcodes later
}
